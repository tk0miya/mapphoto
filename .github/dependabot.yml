version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      timezone: "Asia/Tokyo"
    cooldown:
      default-days: 7

  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
      timezone: "Asia/Tokyo"
    cooldown:
      default-days: 7
    groups:
      # Keep react and react-dom (and their type defs) in lockstep.
      # They must share the exact same version or React throws error #527.
      react:
        patterns:
          - "react"
          - "react-dom"
          - "@types/react"
          - "@types/react-dom"
    ignore:
      # Hold Node.js releases until LTS promotion (October each year).
      # Remove an entry when it enters LTS; add the next April release as it approaches.
      - dependency-name: "@types/node"
        versions: ["25.x", "26.x", "27.x", "28.x"]
